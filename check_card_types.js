const { createConnection } = require('typeorm'); const config = { type: 'postgres', host: 'localhost', port: 5432, username: 'postgres', password: 'hoangpho', database: 'thoi_dai_tho_san_v2' }; createConnection(config).then(conn => { console.log('Connected'); conn.query('SELECT COUNT(*) as count FROM scratch_card_types WHERE is_active = true;').then(rows => { console.log('Active card types count:', rows[0].count); conn.close(); process.exit(0); }); }).catch(err => { console.error(err); process.exit(1); });
