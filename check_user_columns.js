const { createConnection } = require('typeorm'); const config = { type: 'postgres', host: 'localhost', port: 5432, username: 'postgres', password: 'hoangpho', database: 'thoi_dai_tho_san_v2' }; createConnection(config).then(conn => { console.log('Connected'); conn.query('SELECT column_name FROM information_schema.columns WHERE table_name = \'user\' AND table_schema = \'public\';').then(rows => { console.log('User columns:', rows.map(r => r.column_name)); conn.close(); process.exit(0); }); }).catch(err => { console.error(err); process.exit(1); });
