const { createConnection } = require('typeorm'); const config = { type: 'postgres', host: 'localhost', port: 5432, username: 'postgres', password: 'hoangpho', database: 'thoi_dai_tho_san_v2' }; createConnection(config).then(conn => { console.log('Connected'); conn.query('SELECT * FROM user LIMIT 1;').then(rows => { console.log('First user:', Object.keys(rows[0])); conn.close(); process.exit(0); }); }).catch(err => { console.error(err); process.exit(1); });
