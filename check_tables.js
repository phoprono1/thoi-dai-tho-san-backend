const { createConnection } = require('typeorm'); const config = { type: 'postgres', host: 'localhost', port: 5432, username: 'postgres', password: 'hoangpho', database: 'thoi_dai_tho_san_v2' }; createConnection(config).then(conn => { console.log('Connected'); conn.query('SELECT table_name FROM information_schema.tables WHERE table_schema = \'public\';').then(rows => { console.log('Tables:', rows.map(r => r.table_name)); conn.close(); process.exit(0); }); }).catch(err => { console.error(err); process.exit(1); });
