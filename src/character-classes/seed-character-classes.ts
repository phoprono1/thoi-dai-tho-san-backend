import { DataSource } from 'typeorm';

// Minimal seed stub so CLI scripts and builds that import this module succeed.
// If you have a richer seeding process, replace this implementation with the real logic.
export async function seedCharacterClasses(dataSource: DataSource | any) {
  // noop: placeholder for seeding character classes
  // Example real implementation could use dataSource.getRepository(CharacterClass).save([...])
  return;
}
